import { BeautifyDirective } from './beautify.directive';

describe('BeautifyDirective', () => {
  it('should create an instance', () => {
    const directive = new BeautifyDirective();
    expect(directive).toBeTruthy();
  });
});
